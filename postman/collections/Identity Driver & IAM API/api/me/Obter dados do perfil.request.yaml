$kind: http-request
description: Retorna o payload completo do usuário autenticado logado.
url: "{{baseUrl}}/api/me"
method: GET
examples: ./.resources/Obter dados do perfil.resources/examples
order: 3000
